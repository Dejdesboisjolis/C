#include <stdio.h>
#include <stdlib.h>
#include "pyramide.h"

int main(void)
{
    //int resultat = 1;
    //Pyramide();
    //Puissance(2,4,&resultat);
    
/*     Liste *maListe = init();
    insertion(maListe,9);
    insertion(maListe,3);
    insertion(maListe,3);
    insertion(maListe,3);
    insertion(maListe,3);
    afficher(maListe);
    afficher(maListe);
    ajout_milieu(maListe, 7, 3);
    afficher(maListe); */

/*         int i;
    pthread_t tid;
 
    // Let us create three threads
    for (i = 0; i < 3; i++)
        pthread_create(&tid, NULL, myThreadFun, (void *)&tid);
 
    pthread_exit(NULL); */

    //NProc();
    //waitProc();
    //waitProcInOrder();
    //NGenerationProc();
    //sign();
    //tempo();
    //CreatePipe();
    //TwoWaysComPipe();
    //multithreads();
    //return_thread();
    passed_value();

    return 0;


    return EXIT_SUCCESS;
}